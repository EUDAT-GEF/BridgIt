dist: trusty
sudo: required
language: go

before_install:
  - echo ${TRAVIS_BUILD_DIR}
  - echo ${GOPATH}
  - curl https://glide.sh/get | sh
  - go get -u github.com/EUDAT-GEF/GEF || exit 0
  - cd $GOPATH/src/github.com/EUDAT-GEF/GEF
  - make build

script: echo "Hello"